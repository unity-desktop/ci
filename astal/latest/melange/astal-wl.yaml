package:
  name: astal-wl
  version: 0.1.0
  epoch: 0
  description: Central wayland connection manager for the other Astal libraries
  copyright:
    - license: LGPL-2.1-only
  dependencies:
    runtime:
      - glib
      - wayland

environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - glib-dev
      - glib-gir
      - gobject-introspection-dev
      - meson
      - ninja-build
      - pkgconf
      - python-3.13
      - vala
      - wayland-dev
      - wayland-protocols-dev
      - wl-vapi-gen # melange

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/aylur/astal.git
      branch: main
      depth: 1

  # Every Astal library is its own meson project inside the monorepo.
  - working-directory: lib/wl
    pipeline:
      - uses: meson/configure
        with:
          opts: |
            -Dbuildtype=debugoptimized

      - uses: meson/compile

      - uses: meson/install

  - uses: strip

subpackages:
  - name: astal-wl-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - astal-wl # melange
        - glib-dev
        - wayland-dev
        - wayland-protocols-dev

update:
  enabled: false
